from setuptools import setup

setup(
    name="auto_generated_pipieline_framework",
    version="0.0.1",
    description="auto_generated_pipieline_framework",
    setup_requires=["flake8", "pytest-runner"],
    packages=["auto_generated_pipieline_framework"],
    tests_require=["pytest"]
)
